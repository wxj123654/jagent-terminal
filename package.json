{
  "name": "jagent",
  "private": true,
  "workspaces": [
    "packages/*",
    "e2e"
  ],
  "scripts": {
    "setup-refs": "bun run scripts/setup-refs.ts",
    "build": "bun run scripts/build.ts",
    "icons": "bun run scripts/generate-icons.ts",
    "export-patches": "bun run scripts/export-patches.ts",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint"
  },
  "devDependencies": {
    "@resvg/resvg-js": "2.6.2",
    "@types/bun": "^1.4.0",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0"
  }
}
