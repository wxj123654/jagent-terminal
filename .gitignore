# Rust
/target/

# 参考仓库浅克隆（可重建；本地补丁经 patches/ 目录版本化，见 bun run setup-refs）
.refs/

# Node / bun（Phase 1 起）
node_modules/
*.tsbuildinfo

# napi 原生二进制（index.js/index.d.ts 是生成物但入库，供 TS 类型用）
packages/native/*.node
packages/native/*.dll

# 杂项
*.log
.DS_Store
Thumbs.db

# pi agent 本地会话产物
.pi/
