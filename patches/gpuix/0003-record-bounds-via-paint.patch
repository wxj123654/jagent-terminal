diff --git a/packages/native/src/automation.rs b/packages/native/src/automation.rs
index 8a19765..c8e82c9 100644
--- a/packages/native/src/automation.rs
+++ b/packages/native/src/automation.rs
@@ -65,8 +65,29 @@ pub fn bounds_frame_reset() -> impl IntoElement {
 /// such as `gpui::img` cannot have. Wrapping the leaf in a div instead would
 /// move the layout box: the wrapper would become the flex item and the image
 /// would lose intrinsic sizing and corner clipping.
+///
+/// A full-size child would also poison scrolling: gpui derives a scroll
+/// container's scrollable size from `child_max - child_min` over its layout
+/// children, so an `.absolute().size_full()` overlay pins `content_size` to the
+/// container's own size and every scroll container with padding then reports
+/// `padding_size` of phantom scroll range even when nothing overflows.
+/// `on_painted` reports the same box without becoming a layout child.
 pub fn track_own_bounds<E: gpui::InteractiveElement>(el: E, id: u64) -> E {
-    el.on_painted(move |bounds, _, _| record_bounds(id, bounds))
+    track_own_bounds_with(el, id, None)
+}
+
+/// [`track_own_bounds`] plus a selection-start region for the same box.
+pub fn track_own_bounds_with<E: gpui::InteractiveElement>(
+    el: E,
+    id: u64,
+    selection_start: Option<bool>,
+) -> E {
+    el.on_painted(move |bounds, _, _| {
+        record_bounds(id, bounds);
+        if let Some(selectable) = selection_start {
+            crate::text::record_start_region(bounds, selectable);
+        }
+    })
 }
 
 pub fn record_bounds(id: u64, bounds: Bounds<Pixels>) {
@@ -84,20 +105,6 @@ pub fn all_bounds() -> HashMap<u64, ElementBounds> {
     BOUNDS.with(|cell| cell.borrow().clone())
 }
 
-pub fn bounds_tracker(id: u64, selection_start: Option<bool>) -> impl IntoElement {
-    canvas(
-        |bounds, _, _| bounds,
-        move |bounds, _, _, _| {
-            record_bounds(id, bounds);
-            if let Some(selectable) = selection_start {
-                crate::text::record_start_region(bounds, selectable);
-            }
-        },
-    )
-    .absolute()
-    .size_full()
-}
-
 enum ClockMode {
     Live,
     Frozen { now: Instant },
diff --git a/packages/native/src/text/paint.rs b/packages/native/src/text/paint.rs
index 7fa9657..ff29ee1 100644
--- a/packages/native/src/text/paint.rs
+++ b/packages/native/src/text/paint.rs
@@ -114,8 +114,9 @@ pub fn selection_frame_reset(
 
 /// Record a selection-start region from an element's painted box.
 ///
-/// Only `bounds_tracker` calls this, so a start region is always the same box
-/// automation already uses. Last painted region that contains the point wins.
+/// Only [`track_own_bounds_with`](crate::automation::track_own_bounds_with)
+/// calls this, so a start region is always the same box automation already
+/// uses. Last painted region that contains the point wins.
 pub fn record_start_region(bounds: Bounds<gpui::Pixels>, selectable: bool) {
     START_REGIONS.with(|r| r.borrow_mut().push(StartRegion { bounds, selectable }));
 }
