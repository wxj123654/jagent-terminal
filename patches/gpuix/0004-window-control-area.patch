diff --git a/packages/react/src/types/host.ts b/packages/react/src/types/host.ts
index 25d2796..8b42d13 100644
--- a/packages/react/src/types/host.ts
+++ b/packages/react/src/types/host.ts
@@ -174,6 +174,13 @@ export interface StyleDesc {
    *  positioned, but lets the wheel reach the ancestor scroller, like HTML. */
   pointerEvents?: "auto" | "none"
 
+  /** Native window-control marker (custom title bar). `"drag"` = caption:
+   *  Windows resolves WM_NCHITTEST → HTCAPTION and the system handles the
+   *  move; `"close" | "min" | "max"` = the matching caption button (system
+   *  handles the click). On macOS/Linux, drives nothing by itself — pair
+   *  with `renderer.startWindowMove()` in mouse handlers. */
+  windowControlArea?: "drag" | "close" | "min" | "max"
+
   /** "none" opts this element and its subtree out of text selection.
    *  Inherited like the CSS property, so a toolbar can disable it once. */
   userSelect?: "text" | "none" | "auto"
@@ -619,6 +626,12 @@ export interface NativeRenderer {
   setWindowTitle?(title: string): void
   /** Bring the window forward and focus it. Reveals a `show: false` window. */
   activateWindow?(): void
+  /** Begin a user-driven window move from the current pointer position
+   *  (custom title bar drag; macOS/Linux. Windows uses windowControlArea). */
+  startWindowMove?(): void
+  /** Double-click response for a custom-drawn title bar: macOS zooms per the
+   *  system preference, other platforms toggle maximize. */
+  titlebarDoubleClick?(): void
   setDebugFrameOverlay?(mode: DebugFrameOverlayMode): string
   getDebugFrameOverlay?(): string
   cycleDebugFrameOverlay?(): string
